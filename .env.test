# Test environment configuration
DATABASE_URI=postgresql://postgres:password@localhost:5433/findmydoc-test
PAYLOAD_SECRET=test-secret-for-integration-tests
NEXT_PUBLIC_SERVER_URL=http://localhost:3000
CRON_SECRET=test-cron-secret
PREVIEW_SECRET=test-preview-secret

# Logging configuration for tests
# Set to 'silent' to suppress all logs, 'error' to show only errors, 'warn' for warnings, 'info' for info, or 'debug' for verbose
PAYLOAD_LOG_LEVEL=error
# Set to 'true' to suppress hook info logs (like average price updates) during tests
SUPPRESS_HOOK_LOGS=true