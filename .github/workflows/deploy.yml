name: CI & CD

permissions:
  contents: read
  pull-requests: write

# Prevent concurrent runs on the same PR or branch
concurrency:
  group: ${{ github.workflow }}-${{ github.event_name == 'pull_request' && github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

on:
  pull_request:
    branches: [ main ]
    paths-ignore:
      - '!**/.github/workflows/**'
      - '**/.github/dependabot.yml'
      - '**/*.md'
      - '**/.github/instructions/**'
      - '**/.github/prompts/**'
      - '**/copilot-instructions.md'
      - '**/.vscode/settings.json'
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:

env:
  NODE_VERSION: 22
  PNPM_VERSION: 10.12.3
  VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
  VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
  VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
  NEXT_TELEMETRY_DISABLED: 1

defaults:
  run:
    shell: bash

jobs:
  ci:
    name: ✅ Check & Unit Tests
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Restore pnpm store cache
        uses: actions/cache@v4
        with:
          path: ~/.pnpm-store
          key: pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            pnpm-store-

      - name: Install dependencies
        run: pnpm install

      - name: Generate permission matrix JSON
        run: pnpm matrix:derive json

      - name: Verify permission matrix alignment
        run: pnpm matrix:verify

      - name: Check migrations
        run: |
          if git diff --quiet origin/main HEAD -- src/migrations/; then
            echo "No migrations changed."
          else
            echo "🚨 Migration scripts changed!"
          fi

      - name: Lint
        run: pnpm lint

      - name: Run unit tests
        id: vitest
        run: pnpm vitest --project unit --run --coverage

      - name: Publish coverage summary
        if: ${{ steps.vitest.outcome == 'success' }}
        uses: davelosert/vitest-coverage-report-action@v2
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          vite-config-path: .github/coverage/vitest.thresholds.unit.js
          name: Unit Tests
          file-coverage-mode: all
          json-summary-path: coverage/unit/coverage-summary.json
          json-final-path: coverage/unit/coverage-final.json

      - name: Generate job summary
        if: always()
        run: |
          echo "## CI Job Summary" >> $GITHUB_STEP_SUMMARY
          echo "- **Lint**: ${{ steps.vitest.outcome }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Unit Tests**: ${{ steps.vitest.outcome }}" >> $GITHUB_STEP_SUMMARY

  build:
    name: 🔨 Build
    needs: ci
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      PAYLOAD_SECRET: ${{ secrets.PAYLOAD_SECRET }}
      CRON_SECRET: ${{ secrets.CRON_SECRET }}
      PREVIEW_SECRET: ${{ secrets.PREVIEW_SECRET }}
      DATABASE_URI: postgresql://postgres:password@localhost:5432/findmydoc-portal
    services:
      postgres:
        image: postgis/postgis:15-3.3
        ports:
          - 5432:5432
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: password
          POSTGRES_DB: findmydoc-portal
        options: >-
          --health-cmd "pg_isready -U postgres -h localhost -d postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Restore pnpm store cache
        uses: actions/cache@v4
        with:
          path: ~/.pnpm-store
          key: pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            pnpm-store-

      - name: Install dependencies
        run: pnpm install

      # Note: Permission matrix already verified in CI job, skip here to save time

      - name: Wait for PostgreSQL service
        run: |
          sudo apt-get update && sudo apt-get install -y postgresql-client
          echo "Waiting for PostgreSQL to be ready on localhost:5432..."
          for i in {1..30}; do
            if nc -z localhost 5432 && pg_isready -h localhost -p 5432 -U postgres -q; then
              echo "PostgreSQL is ready!"
              exit 0
            fi
            echo "Attempt $i: PostgreSQL not yet ready. Waiting 2 seconds..."
            sleep 2
          done
          echo "Error: PostgreSQL did not become ready in time."
          exit 1

      - name: Run database migrations
        run: |
          echo "Running database migrations..."
          pnpm run migrate

      - name: Build application (static check)
        run: pnpm build

      - name: Run integration tests
        id: integration_tests
        env:
          NODE_ENV: test
        run: pnpm vitest --project integration --run --coverage --reporter=verbose

      - name: Publish coverage summary
        if: ${{ steps.integration_tests.outcome == 'success' }}
        uses:  davelosert/vitest-coverage-report-action@v2
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          vite-config-path: .github/coverage/vitest.thresholds.integration.js
          name: Integration Tests
          file-coverage-mode: all
          json-summary-path: coverage/integration/coverage-summary.json
          json-final-path: coverage/integration/coverage-final.json

      - name: Generate job summary
        if: always()
        run: |
          echo "## Build Job Summary" >> $GITHUB_STEP_SUMMARY
          echo "- **Build**: ${{ job.status }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Integration Tests**: ${{ steps.integration_tests.outcome }}" >> $GITHUB_STEP_SUMMARY


  Deploy-Preview:
    name: Deploy Preview
    if: github.event_name == 'pull_request' || github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs:
      - build
    environment:
      name: Preview
      url: ${{ steps.vercel_preview.outputs.deploymentUrl }}
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install

      - name: Cache Vercel CLI
        uses: actions/cache@v4
        with:
          path: ~/.npm/_npx
          key: vercel-cli-${{ runner.os }}
          restore-keys: |
            vercel-cli-

      - name: Install Vercel CLI
        run: pnpm install --global vercel@canary

      - name: Pull Vercel Environment Variables for Preview
        run: vercel pull --yes --environment=preview --token=${{ secrets.VERCEL_TOKEN }}

      - name: Deploy to Vercel (Preview)
        id: vercel_preview
        run: |
          vercel deploy --target preview --token=${{ secrets.VERCEL_TOKEN }} >deployment-url.txt 2>error.txt

          code=$?
          if [ $code -eq 0 ]; then
              deploymentUrl=$(cat deployment-url.txt)
              echo "deploymentUrl=$deploymentUrl" >> "$GITHUB_OUTPUT"
              echo "✅ Preview deployment successful: $deploymentUrl"
          else
              echo "❌ There was an error during Vercel deployment:"
              cat error.txt
              exit $code
          fi

      # fixed url has to be manually set because Vercel does not automatically set it for preview deployments
      - name: Set Preview Alias
        run: |
          vercel alias set ${{ steps.vercel_preview.outputs.deploymentUrl }} findmydoc-portal-tst.vercel.app --token=${{ secrets.VERCEL_TOKEN }}

      - name: Generate deployment summary
        if: always()
        run: |
          echo "## Preview Deployment Summary" >> $GITHUB_STEP_SUMMARY
          if [ "${{ steps.vercel_preview.outcome }}" == "success" ]; then
            echo "✅ **Status**: Success" >> $GITHUB_STEP_SUMMARY
            echo "🔗 **URL**: ${{ steps.vercel_preview.outputs.deploymentUrl }}" >> $GITHUB_STEP_SUMMARY
            echo "🔗 **Alias**: https://findmydoc-portal-tst.vercel.app" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ **Status**: Failed" >> $GITHUB_STEP_SUMMARY
          fi

  Deploy-Production:
    name: Deploy Production
    if: github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs:
      - build
      - Deploy-Preview
    environment:
      name: Production
      url: ${{ steps.vercel_production.outputs.deploymentUrl }}
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install

      - name: Cache Vercel CLI
        uses: actions/cache@v4
        with:
          path: ~/.npm/_npx
          key: vercel-cli-${{ runner.os }}
          restore-keys: |
            vercel-cli-

      - name: Install Vercel CLI
        run: pnpm install --global vercel@canary

      - name: Pull Vercel Environment Variables for Production
        run: vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}

      - name: Deploy to Vercel (Production)
        id: vercel_production
        run: |
          vercel deploy --prod --token=${{ secrets.VERCEL_TOKEN }} >deployment-url.txt 2>error.txt

          # check the exit code
          code=$?
          if [ $code -eq 0 ]; then
              # Now you can use the deployment url from stdout for the next step of your workflow
              deploymentUrl=$(cat deployment-url.txt)
              echo "deploymentUrl=$deploymentUrl" >> "$GITHUB_OUTPUT"
              echo "✅ Production deployment successful: $deploymentUrl"
          else
              # Handle the error
              errorMessage=$(cat error.txt)
              echo "❌ There was an error: $errorMessage"
              exit $code
          fi

      - name: Generate deployment summary
        if: always()
        run: |
          echo "## Production Deployment Summary" >> $GITHUB_STEP_SUMMARY
          if [ "${{ steps.vercel_production.outcome }}" == "success" ]; then
            echo "✅ **Status**: Success" >> $GITHUB_STEP_SUMMARY
            echo "🔗 **URL**: ${{ steps.vercel_production.outputs.deploymentUrl }}" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ **Status**: Failed" >> $GITHUB_STEP_SUMMARY
          fi
